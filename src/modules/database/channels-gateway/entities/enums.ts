export enum ChannelDirection {
  ACTIVE = 'active',
  RECEPTIVE = 'receptive',
  BOTH = 'both',
}

export enum ChannelConfigStatus {
  ACTIVE = 'active',
  DRAFT = 'draft',
  INACTIVE = 'inactive',
}
